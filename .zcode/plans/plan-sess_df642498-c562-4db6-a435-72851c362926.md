# MarxReader 优化实施（按批次执行，从批次 A 开始）

已获你确认的总体方案，执行时遵守你的四点补充：
1. **B-6 退出落库**：`runBlocking + withTimeoutOrNull` 方案谨慎处理——仅在 onDispose（退出阅读页）时短超时等待，onPause 保持纯异步；若实测有任何卡顿风险则改为「退出时取消节流延迟、立即入队 + 尽快落库」的折中，不引入生命周期阻塞。
2. **C-13 缓存上限**：不简单翻倍。先评估 ReaderPage 实际内存（字符串 + 注记对象），再按设备内存档位（ActivityManager.memoryClass / isLowRamDevice）动态调整权重上限。
3. **B-7 revision 拆分**：独立成一个阶段单独实施，重点回归验证阅读进度保存、书架刷新、目录章节状态。
4. 不做任何未授权重构：不改 JSON 数据结构、不迁移包目录。

## 当前批次：A 崩溃防护（8 文件 + 1 新测试）

1. `ReadingSessionTracker.kt`：会话协程整体 try/catch，统计写库失败静默丢弃（正常路径零变化）
2. `LibraryRepository.kt`：`resolveNotes` 失败→空列表+「笔记加载失败」提示；`readBook` 坏 JSON 降级为 null；FTS 索引构建跳过坏书，全部失败才报错
3. `ReadingScreen.kt`：错误展示 `!!` 改局部智能转换；TOC 章节进度查询包 readerOperation，失败保留旧值
4. `ReaderApp.kt`：初始化错误 `!!` 同上；书库页/作者页/书籍详情页 3 处进度读取包 readerOperation
5. `SearchScreen.kt`：搜索错误 `!!` 改智能转换
6. `ReaderPagination.kt`：幻影行防护（startChar ≥ styled.length 结束分页；endChar 收敛到合法区间）
7. `Models.kt` + `TocTree.kt`：目录父链/树构建遍历加 visited 防环；TOC 节点按 id 去重
8. 新增 `CatalogSafetyTest.kt`：防环/去氪回归测试（JUnit timeout 防挂起）

完成后运行 `gradlew testDebugUnitTest` 验证，向你汇报批次 A 修改清单与测试结果，**确认后再进入批次 B**。不自动 git commit。

后续批次（等你逐批确认）：B 可靠性（其中 revision 拆分独立阶段）→ C 性能 → D 零风险杂项。