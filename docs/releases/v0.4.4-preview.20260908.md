# 0.4.4-preview.20260908

- versionCode：12，沿用马克思头像。
- 阅读工具栏覆盖在固定正文区域上，开合菜单不再改变分页尺寸；底部预留导航空间。
- 滚动模式按原文字符和实际文字行恢复，支持长段落续读及阅读模式切换。
- 横向滑动可进入上一章或下一章；滚动模式增加章末入口。
- 点击搜索结果收起大面板，保留轻量结果导航；搜索和目录跳转后可返回原位。
- 当前书籍缓存最多三份章节排版，总量最多 60 万文本字符；预排下一章，跳过超大章节的预排。
- 分页计算移至后台，连续排版调整合并，过期任务在计算检查点取消；滚动模式不生成分页。
- 合并连续阅读位置写入，离开阅读器和切入后台时立即提交最新位置；目录关闭时不重复查询章节进度。
- 缓存段落和章节字符前缀，阅读百分比无需反复遍历整本书；避免重复设置原生文字样式与窗口属性。
- 统一分页和原生正文显示的行距、标题字号及段间距。

## 验证

41 项单元测试全部通过；Debug 构建、Android 测试 APK 构建及 lint 通过。lint 保留已有的单色主题图标警告。新增段内定位、跨章边界、缓存容量与淘汰、进度索引兼容测试；新增原生分页高度和字体变化定位的设备测试。

此环境没有已连接的 Android 设备，设备测试尚未运行，未测量真机帧率、峰值内存或耗电。需要在安装预览版后验收：菜单开合、长段落退出重进、模式切换、首末章滑动、搜索返回、大字号与屏幕旋转。

预览版使用 Debug 签名，不是正式发布签名包。
